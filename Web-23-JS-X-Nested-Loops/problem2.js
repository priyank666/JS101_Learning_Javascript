// chunnu is asking munnu to plant seeds in the 10 farms that they have but in horizontal manner.

for (let farm = 1; farm <= 10; farm++){

  let bag = "";
  
  for (let seed = 1; seed <= 5; seed++){

    bag += "*" + " ";
  }
  console.log(bag);
}
